# Recommendation approvals

This directory contains reviewed, generation-bound approval receipts. It is
intentionally empty while recommendation remains blocked. A receipt must pass
`npm run knowledge:verify-recommendation-approval` before it can affect a gate.
