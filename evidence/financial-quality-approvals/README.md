# Financial quality approvals

This directory intentionally contains no approval receipt until a human reviewer
has run the candidate-bound staging suite and signed the resulting receipt.

The receipt shape is defined by `schemas/financial-quality-approval-receipt.schema.json`.
